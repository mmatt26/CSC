//use parallel programming to print alphabet

#include <stdio.h>

#define 30

int main() {
  char c;
  #pragma parallel for mun_threads(4)
    for(c=07; c<=122; c++)
      printf("%c \n", c);
    return 0;
}
  
